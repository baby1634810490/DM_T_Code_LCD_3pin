# TSDA 速度模式领域术语

## 模块边界

**TSDA 协议 Driver**：
只表达厂家 CAN 帧、寄存器和回包匹配规则的硬件无关模块。Driver 不读取配置文件，不区分 Chassis 或 3Pin。
_避免使用_：Chassis Driver、3Pin Driver、CAN1 Driver

**TSDA App**：
负责初始化、使能、寻限和正常运动时序的非阻塞状态机。当前按可上机验证的阶段逐步增加功能。
_避免使用_：一次性完整重写

**CAN1 适配器**：
`task_motor.c` 中把 Driver 发送函数映射到现有 `FDCAN1_Send_Msg()` 的板级接口。CAN1 接收回调继续调用唯一的 `TSDA_AppOnCanRx()`。
_避免使用_：Driver 直接调用 FDCAN1、增加 TSDA 接收函数

**控制数据责任边界**：
通信层负责通信中断、异常值和非法输入；TSDA 电机执行层只消费已经有效的控制数据，不重复校验通信输入。
_避免使用_：执行层通信故障处理、执行层输入清洗

## 控制模式

**全速度模式**：
使能、自动寻限、目标位置运动和零速保持均使用驱动器速度 PC 模式。驱动器目标速度寄存器为 `0x10`，单位为有符号 RPM。
_避免使用_：位置模式寻限、位置模式保持、动态位置速度上限

**速度模式配置**：
写 `0x02=0x00C4`，并写 `0x0A=0x0101`。驱动器内部加减速均采用最短时间1，后续正常运动由 MCU 完成外层速度规划。

**Chassis 向上方向**：
Chassis 电机负转速驱动滑台向上，正转速驱动滑台向下；该方向关系以实机测试结果为准。
_避免使用_：正转速向上、根据旧代码推断转速方向

**滑台用户坐标**：
物理上限位置定义为 `0mm`，向下为负方向，软件下限位置为 `-300mm`。
_避免使用_：向下为正的用户高度、把驱动器原始计数直接作为滑台高度

**滑台软件行程边界**：
目标高度被限制在 `[0,-300]mm`，实际位置到达边界后不得继续向边界外运动，但允许反向离开边界。
_避免使用_：只钳位目标而允许实际运动继续越界、使用物理下限参与正常控制

**滑台坐标速度**：
滑台当前速度是用户高度对时间的变化率，向上为正、向下为负；其符号与驱动器 E4 转速符号相反。
_避免使用_：无符号速度、直接把 E4 转速符号作为用户速度符号

**零速保持**：
伺服保持使能并持续下发 `0RPM`。目标附近允许 `2~3mm` 静态误差，不要求受外力偏移后精确返回原位置。
_避免使用_：位置锁定、精确位置保持

**零速保持回差**：
后续目标位置控制中，位置误差进入 `±2mm` 时下发零速；保持期间只有误差超过 `±3mm` 才重新开始规划。

**滑台实时控制**：
寻限和自动回位完成后，绝对目标高度、最大速度和加速度按最新协议值实时参与下一次速度规划。
_避免使用_：增量高度命令、运动开始时一次性锁存参数

**滑台有效反馈**：
软件零点建立且自动回位停稳前，高度和速度反馈都为零；`ready=1` 后反馈真实浮点高度和带符号坐标速度。
_避免使用_：寻限前的原始位置高度、无符号速度反馈

## 分阶段实现

**第一阶段 - Chassis 零速使能**：
清故障、配置速度模式、写 `0RPM`、使能，然后按3相节拍循环执行“写0RPM、读E8/E9、读E4”。本阶段不寻限、不建立软件零点、不接受目标高度，`ready` 固定为0。

**第二阶段 - Chassis 上限寻限**：
使能稳定后先读 `0x58` 和 E8/E9 寻限起点。上限未触发时按3相节拍执行“写负转速、读E8/E9、读 `0x58`”；上限触发后写 `0RPM`，等待50ms稳定，再读取停止位置作为软件零点。累计移动达到 `300mm` 或寻限达到 `90s` 时，同样先零速停止，再进入 ERROR。

**逐步实测**：
每次只增加一个可以独立观察的功能阶段；上一阶段实机通过后才开始下一阶段。Chassis 完整速度模式通过后，再接入 3Pin 的 MCU 抱闸和板级限位能力。
